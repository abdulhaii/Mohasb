import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.abdulhaii.mohasb',
  appName: 'محاسب',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
